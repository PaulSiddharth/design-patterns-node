class Movie {
    constructor(movieId, title, genre, language, duration, city) {
        this.movieId = movieId;
        this.title = title;
        this.genre = genre;
        this.language = language;
        this.duration = duration;
        this.city = city;
    }
}

module.exports = Movie